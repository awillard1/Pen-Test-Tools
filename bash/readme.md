```
for x in `ls`;do cd $x;ls -la; cat .bash_history;cd /home;done
for x in `ls`;do cd $x;ls -la; cat .mysql_history;cd /home;done
for x in `ls`;do cd $x;ls -la; cat .viminfo;cd /home;done
for x in `ls`;do cd $x;ls -la; cat .sh_history;cd /home;done
```
